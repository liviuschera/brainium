import styled from 'styled-components';

export const ImageLinkFormWrapper = styled.form`
  width: 50%;
  background-color: transparent;
  box-shadow: 0 1.5rem 3rem 1rem rgba(0, 0, 0, 0.3);
  position: absolute;
  top: 50%;
  left: 50%;
  margin-right: -50%;
  transform: translate(-50%, -50%);
  border-radius: 0.5rem;
`;
